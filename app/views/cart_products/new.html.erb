<%= form_with model: @cart_product do |f| %>
  <%= f.hidden_field :id, value: @cart_product.product_id %>
  <%= f.label :quantity %>
  <%= f.text_field :quantity %>
  <%= f.submit %>
<% end %>
<div>
  <%= link_to 'Back', root_path %>
</div>
